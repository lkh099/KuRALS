{
 "model": "kuralsnet_npu",
 "dataset": "KuRALS_CW",
 "process_signal": true,
 "w_size": 256,
 "h_size": 256,
 "nb_input_channels": 1,
 "nb_epochs": 100,
 "batch_size": 6,
 "lr": 0.0001,
 "grad_clip_norm": null,
 "lr_step": 20,
 "schedular": "cos",
 "Tmax": 100,
 "loss_step": 100,
 "val_step": 2000,
 "torch_seed": 42,
 "numpy_seed": 42,
 "version": 0,
 "device": "cuda",
 "heatmap_weight": 1.0,
 "offset_weight": 8.0,
 "heatmap_pos_weight": 2.0,
 "gamma": 3.0,
 "quant_warmup_iters": 500,
 "quant_freeze_iters": 5000,
 "lr_warmup_iters": 500,
 "lr_warmup_start_factor": 0.1,
 "transformations": "hflip,vflip",
 "norm_type": "tvt",
 "shuffle": true,
 "num_workers": 1,
 "comments": "Retired CenterNet-style detection head config -- see kurals/legacy/README.md. Iteratively tuned through 24 revisions (channel widths, output stride, Gaussian-splat target radius, temporal frame-stacking, dual-scale heads, quantization warmup/freeze timing, focal-loss gamma/beta, offset weight, weight decay, LR schedule, gradient clipping). Best recorded result: Pd 35.0% at FAR 2.26% (threshold 0.05) on KuRALS_CW -- well below CFAR's 61-88% Pd and baseline kuralsnet's precision/recall. Every further lever tried regressed or collapsed, which motivated replacing this sparse single-point-supervision head with the dense per-pixel segmentation head in kurals/models/kuralsnet_npu_seg.py, which reaches substantially better numbers (see that model's config, kuralsnet_npu_seg.json).",
 "heatmap_loss_type": "focal",
 "weight_decay": 0.0
}
